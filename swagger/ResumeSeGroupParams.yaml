# Copyright 2021 VMware, Inc.
# SPDX-License-Identifier: Apache License 2.0

# GENERATED FILE - DO NOT EDIT THIS FILE
---
swagger: '2.0'
info:
    version: '32.2.1'
    title: Avi ResumeSeGroupParams Object API
    contact:
        name: Avi Networks Inc.
        url: https://avinetworks.com/contact-us
        email: support@avinetworks.com
    description: |
        Avi ResumeSeGroupParams object api
        ```
        - resume segroup
        ```

        API permission: PERMISSION_UPGRADE_OPS
tags:
  - name: ResumeSeGroupParams
schemes:
  - https
securityDefinitions:
    basicAuth:
        type: basic
        description: basic authentication
basePath: /api
paths:

    /upgrade/segroup/resume:
        post:
            tags:
                - ResumeSeGroupParams
            description: /upgrade/segroup/resume
            operationId: POST /upgrade/segroup/resume
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-Avi-Version
                    in: header
                    type: string
                    required: true
                    description: The caller is required to set Avi Version Header to the expected version of configuration. The response from the controller will provide and accept data according to the specified version. The controller will reject POST and PUT requests where the data is not compatible with the specified version.
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller will reject requests.
                -   in: body
                    name: body
                    description: ResumeSeGroupParams
                    required: true
                    schema:
                        $ref: "#/definitions/ResumeSeGroupParams"
            security:
                - basicAuth: []
            responses:
                "200":
                    description: OK
                    schema:
                        type: string
                "401":
                    description: log in failed

definitions:
    ResumeSeGroupParams:
        type: object
        properties:
            prechecks_only:
                description: "This flag is set to run the pre-checks without the subsequent upgrade operations. Field introduced in 22.1.6, 30.2.1. Allowed with any value in Enterprise, Essentials, Basic, Enterprise with Cloud Services edition."
                type: boolean
            se_group_options:
                description: "SE Group options for resume operations. Field introduced in 18.2.6. Allowed with any value in Enterprise, Essentials, Basic, Enterprise with Cloud Services edition."
                $ref: '#/definitions/SeGroupResumeOptions'
            se_group_refs:
                description: "SE Group uuids for resuming upgrade. It is a reference to an object of type ServiceEngineGroup. Field introduced in 18.2.6. Minimum of 1 items required. Allowed with any value in Enterprise, Essentials, Basic, Enterprise with Cloud Services edition."
                type: array
                items:
                    type: string
            skip_warnings:
                description: "This is flag when set as true skips few optional must checks. Field introduced in 18.2.6. Allowed with any value in Enterprise, Essentials, Basic, Enterprise with Cloud Services edition."
                type: boolean
    SeGroupResumeOptions:
        type: object
        properties:
            action_on_error:
                description: "The error recovery action configured for a SE Group. Enum options - ROLLBACK_UPGRADE_OPS_ON_ERROR, SUSPEND_UPGRADE_OPS_ON_ERROR, CONTINUE_UPGRADE_OPS_ON_ERROR. Field introduced in 18.2.6. Allowed with any value in Enterprise, Essentials, Basic, Enterprise with Cloud Services edition."
                type: string
                default: "SUSPEND_UPGRADE_OPS_ON_ERROR"
            disruptive:
                description: "Allow disruptive mechanism. Field introduced in 18.2.8, 20.1.1. Allowed with any value in Enterprise, Essentials, Basic, Enterprise with Cloud Services edition."
                type: boolean
            skip_suspended:
                description: "Skip upgrade on suspended SE(s). Field introduced in 18.2.6. Allowed with any value in Enterprise, Essentials, Basic, Enterprise with Cloud Services edition."
                type: boolean
